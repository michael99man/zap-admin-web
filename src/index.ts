import { createElement } from 'react';
import { render } from 'react-dom';
import { App } from './app/Main';


render(createElement(App, {}, null), document.getElementById('app'));
// const mnemonic = 'silver traffic ready ocean horror shaft foil miss code ribbon liberty glove';
// mnemonic :  "rally later assist feature wait primary addict sister remove language piec drink"
// mnemonic :  "pact inside track layer hello carry used silver pyramid bronze drama time"